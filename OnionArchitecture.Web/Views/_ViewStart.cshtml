
@* The _ViewStart file can be used to define common view code
    that you want to execute at the start of each View’s rendering.
    Specifically, it is used to specifify the path to the default layout of the page. *@

@{
    Layout = "_Layout";
}
